name: release
on: push

#on:
#  release:
#    types:
#      - created

jobs:
  gcs-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: release gcs
        env:
          GCS_CREDS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
        run: |
          ls
          docker run --rm golang@sha256:5219b39d2d6bf723fb0221633a0ff831b0f89a94beb5a8003c7ff18003f48ead
